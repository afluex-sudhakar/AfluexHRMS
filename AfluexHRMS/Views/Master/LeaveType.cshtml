@model AfluexHRMS.Models.Master

@{
    ViewBag.Title = "LeaveType";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}

@using (Html.BeginForm("LeaveType", "Master", FormMethod.Post, new { @id = "form1", enctype = "multipart/form-data" }))
{
    <div class="container-fluid">
        <div class="row page-titles">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="javascript:void(0)">Master</a></li>
                <li class="breadcrumb-item active"><a href="javascript:void(0)">Leave Type</a></li>
            </ol>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header d-lg-flex justify-content-between">
                        <h4 class="card-title">Leave Type</h4>
                        <p style="color: #ff0000">
                            @TempData["Leavetypemsg"]
                        </p>
                        <p style="color: red">
                            @TempData["ErrLeavetypemsg"]
                        </p>
                    </div>
                    <div class="white_card_body">
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label"><h5>Leave Type <span class="text-danger">*</span></h5></label>
                                    @Html.TextBoxFor(m => m.LeaveType, new { @class = "form-control", @Placeholder = "Leave Type" })

                                    @Html.HiddenFor(m => m.Pk_LeaveTypeId)
                                </div>

                                <div class="col-md-6 mb-3">
                                    <label class="form-label"><h5>Leave Limit <span class="text-danger">*</span></h5></label>
                                    @Html.TextBoxFor(m => m.LeaveLimit, new { @class = "form-control", @Placeholder = "Leave Limit" })
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label" for="inputEmail4"><h5>Remark</h5></label>
                                    @Html.TextAreaFor(m => m.Remark, new { @class = "form-control", @Placeholder = "Remark", @rows = "5", @cols = "5" })
                                </div>
                                <div class="col-md-3 mb-3">
                                    <label class="form-label d-block mb-lg-4">&nbsp;</label>
                                    @if (Model.Pk_LeaveTypeId == null)
                                    {
                                        <input type="submit" class="btn btn-success" id="btnSave" onclick="return validateform();" name="btnSave" value="Save" />
                                    }
                                    else
                                    {

                                        <input type="submit" class="btn btn-success" onclick="return validateform();" id="btnUpdate" name="btnUpdate" value="Update" />
                                    }

                                </div>

                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </div>

}

<script>
    function validateform() {
        $(".errortext").removeClass("errortext");
        if ($('#LeaveType').val() == '') {
            $("#LeaveType").addClass('errortext');
            $('#LeaveType').focus();
            return false;
        }

        if ($('#LeaveLimit').val() == '') {
            $("#LeaveLimit").addClass('errortext');
            $('#LeaveLimit').focus();
            return false;
        }
        return true;
    }
</script>



<style>
    .errortext {
        border: 1px solid red !important;
        background-color: #fff0f0 !important;
    }
</style>